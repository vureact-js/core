# Vureact Generated Project

This is a React project **automatically generated by the Vureact Compiler**. It has converted the original Vue 3 logic into a React runtime.

## 🛠 Basic Tech Stack

This generated output is based on the following modern frontend tech stack:

- **Framework**: React 18+
- **Build Tool**: Vite 5
- **Language**: TypeScript
- **Runtime Glue**: `@vureact/runtime` (provides core Vue 3 API to React adaptation package)

---

## ⚠️ Important Notes

**Please read carefully before starting development:**

1. **Basic Runtime Guarantee**:
   Files in this directory (`package.json`, `vite.config.ts`, `tsconfig.json`, etc.) only ensure that a standard React environment can start normally.

2. **Manual Synchronization of External Dependencies**:
   **Very Important**: Vureact only handles code logic conversion. If your original Vue project uses additional third‑party libraries (e.g., `axios`, `lodash`, `dayjs`, `ant-design`, `echarts`, etc.), **you must manually run `npm install <package-name>` in this directory to install these dependencies or their React equivalents.**

3. **Manual Migration of Environment Configurations**:
   If the original project has custom environment variables (`.env`), Vite plugin configurations, path aliases, ESLint, Less, or other settings, please manually modify `vite.config.ts` or related configuration files as needed.

---

## 🚀 Quick Start

### 1. Install Dependencies

Enter this directory and install base and business dependencies:

```bash
npm install
# Or use pnpm / yarn
# pnpm install
```

### 2. Start Dev Server

```bash
npm run dev
```

### 3. Build for Production

```bash
npm run build
```

---

This project was automatically generated by Vureact. If you plan to re‑run the compiler, **do not modify the core logic directly in src/**, as changes will be overwritten.
