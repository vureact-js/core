[
  {
    "level": "error",
    "message": "The useVRef hook must be called at the top level, not inside loops, conditions, or nested functions.",
    "file": "__fixtures__/parse/script-only/input.ts",
    "line": 7,
    "column": 14,
    "source": "// 测试 Vue Composition API 转换为 React Hooks\n// 注意，这样的写法转成 React 完全不符合规则，仅测试用。\n\nimport { computed, reactive, ref } from 'vue';\n\n// 测试 ref\nconst count = ref(0);\nconst message = ref('Hello');\n\n// 测试 computed\nconst doubleCount = computed(() => count.value * 2);\n\n// 测试 reactive\nconst state = reactive({\n  name: 'Test',\n  items: [1],\n});\n\n// 测试函数\nfunction increment() {\n  count.value++;\n  state.items.push(count.value);\n}\n\n// 测试 watchEffect\nimport { watchEffect } from 'vue';\n\nwatchEffect(() => {\n  console.log('Count changed:', count.value);\n});\n\n// 测试生命周期\nimport { onMounted, onUnmounted } from 'vue';\n\nonMounted(() => {\n  console.log('Component mounted');\n});\n\nonUnmounted(() => {\n  console.log('Component unmounted');\n});\n\n// 测试 defineProps 和 defineEmits\nconst props = defineProps({\n  title: String,\n  count: Number,\n});\n\nconst emit = defineEmits(['update', 'delete']);\n\nfunction handleUpdate() {\n  emit('update', { value: count.value });\n}\n\n// 导出供测试\nexport { count, doubleCount, emit, handleUpdate, increment, message, props, state };\n"
  },
  {
    "level": "error",
    "message": "The useVRef hook must be called at the top level, not inside loops, conditions, or nested functions.",
    "file": "__fixtures__/parse/script-only/input.ts",
    "line": 8,
    "column": 16,
    "source": "// 测试 Vue Composition API 转换为 React Hooks\n// 注意，这样的写法转成 React 完全不符合规则，仅测试用。\n\nimport { computed, reactive, ref } from 'vue';\n\n// 测试 ref\nconst count = ref(0);\nconst message = ref('Hello');\n\n// 测试 computed\nconst doubleCount = computed(() => count.value * 2);\n\n// 测试 reactive\nconst state = reactive({\n  name: 'Test',\n  items: [1],\n});\n\n// 测试函数\nfunction increment() {\n  count.value++;\n  state.items.push(count.value);\n}\n\n// 测试 watchEffect\nimport { watchEffect } from 'vue';\n\nwatchEffect(() => {\n  console.log('Count changed:', count.value);\n});\n\n// 测试生命周期\nimport { onMounted, onUnmounted } from 'vue';\n\nonMounted(() => {\n  console.log('Component mounted');\n});\n\nonUnmounted(() => {\n  console.log('Component unmounted');\n});\n\n// 测试 defineProps 和 defineEmits\nconst props = defineProps({\n  title: String,\n  count: Number,\n});\n\nconst emit = defineEmits(['update', 'delete']);\n\nfunction handleUpdate() {\n  emit('update', { value: count.value });\n}\n\n// 导出供测试\nexport { count, doubleCount, emit, handleUpdate, increment, message, props, state };\n"
  },
  {
    "level": "error",
    "message": "The useComputed hook must be called at the top level, not inside loops, conditions, or nested functions.",
    "file": "__fixtures__/parse/script-only/input.ts",
    "line": 11,
    "column": 20,
    "source": "// 测试 Vue Composition API 转换为 React Hooks\n// 注意，这样的写法转成 React 完全不符合规则，仅测试用。\n\nimport { computed, reactive, ref } from 'vue';\n\n// 测试 ref\nconst count = ref(0);\nconst message = ref('Hello');\n\n// 测试 computed\nconst doubleCount = computed(() => count.value * 2);\n\n// 测试 reactive\nconst state = reactive({\n  name: 'Test',\n  items: [1],\n});\n\n// 测试函数\nfunction increment() {\n  count.value++;\n  state.items.push(count.value);\n}\n\n// 测试 watchEffect\nimport { watchEffect } from 'vue';\n\nwatchEffect(() => {\n  console.log('Count changed:', count.value);\n});\n\n// 测试生命周期\nimport { onMounted, onUnmounted } from 'vue';\n\nonMounted(() => {\n  console.log('Component mounted');\n});\n\nonUnmounted(() => {\n  console.log('Component unmounted');\n});\n\n// 测试 defineProps 和 defineEmits\nconst props = defineProps({\n  title: String,\n  count: Number,\n});\n\nconst emit = defineEmits(['update', 'delete']);\n\nfunction handleUpdate() {\n  emit('update', { value: count.value });\n}\n\n// 导出供测试\nexport { count, doubleCount, emit, handleUpdate, increment, message, props, state };\n"
  },
  {
    "level": "error",
    "message": "The useReactive hook must be called at the top level, not inside loops, conditions, or nested functions.",
    "file": "__fixtures__/parse/script-only/input.ts",
    "line": 14,
    "column": 14,
    "source": "// 测试 Vue Composition API 转换为 React Hooks\n// 注意，这样的写法转成 React 完全不符合规则，仅测试用。\n\nimport { computed, reactive, ref } from 'vue';\n\n// 测试 ref\nconst count = ref(0);\nconst message = ref('Hello');\n\n// 测试 computed\nconst doubleCount = computed(() => count.value * 2);\n\n// 测试 reactive\nconst state = reactive({\n  name: 'Test',\n  items: [1],\n});\n\n// 测试函数\nfunction increment() {\n  count.value++;\n  state.items.push(count.value);\n}\n\n// 测试 watchEffect\nimport { watchEffect } from 'vue';\n\nwatchEffect(() => {\n  console.log('Count changed:', count.value);\n});\n\n// 测试生命周期\nimport { onMounted, onUnmounted } from 'vue';\n\nonMounted(() => {\n  console.log('Component mounted');\n});\n\nonUnmounted(() => {\n  console.log('Component unmounted');\n});\n\n// 测试 defineProps 和 defineEmits\nconst props = defineProps({\n  title: String,\n  count: Number,\n});\n\nconst emit = defineEmits(['update', 'delete']);\n\nfunction handleUpdate() {\n  emit('update', { value: count.value });\n}\n\n// 导出供测试\nexport { count, doubleCount, emit, handleUpdate, increment, message, props, state };\n"
  },
  {
    "level": "error",
    "message": "The useWatchEffect hook must be called at the top level, not inside loops, conditions, or nested functions.",
    "file": "__fixtures__/parse/script-only/input.ts",
    "line": 28,
    "column": 0,
    "source": "// 测试 Vue Composition API 转换为 React Hooks\n// 注意，这样的写法转成 React 完全不符合规则，仅测试用。\n\nimport { computed, reactive, ref } from 'vue';\n\n// 测试 ref\nconst count = ref(0);\nconst message = ref('Hello');\n\n// 测试 computed\nconst doubleCount = computed(() => count.value * 2);\n\n// 测试 reactive\nconst state = reactive({\n  name: 'Test',\n  items: [1],\n});\n\n// 测试函数\nfunction increment() {\n  count.value++;\n  state.items.push(count.value);\n}\n\n// 测试 watchEffect\nimport { watchEffect } from 'vue';\n\nwatchEffect(() => {\n  console.log('Count changed:', count.value);\n});\n\n// 测试生命周期\nimport { onMounted, onUnmounted } from 'vue';\n\nonMounted(() => {\n  console.log('Component mounted');\n});\n\nonUnmounted(() => {\n  console.log('Component unmounted');\n});\n\n// 测试 defineProps 和 defineEmits\nconst props = defineProps({\n  title: String,\n  count: Number,\n});\n\nconst emit = defineEmits(['update', 'delete']);\n\nfunction handleUpdate() {\n  emit('update', { value: count.value });\n}\n\n// 导出供测试\nexport { count, doubleCount, emit, handleUpdate, increment, message, props, state };\n"
  },
  {
    "level": "error",
    "message": "The useMounted hook must be called at the top level, not inside loops, conditions, or nested functions.",
    "file": "__fixtures__/parse/script-only/input.ts",
    "line": 35,
    "column": 0,
    "source": "// 测试 Vue Composition API 转换为 React Hooks\n// 注意，这样的写法转成 React 完全不符合规则，仅测试用。\n\nimport { computed, reactive, ref } from 'vue';\n\n// 测试 ref\nconst count = ref(0);\nconst message = ref('Hello');\n\n// 测试 computed\nconst doubleCount = computed(() => count.value * 2);\n\n// 测试 reactive\nconst state = reactive({\n  name: 'Test',\n  items: [1],\n});\n\n// 测试函数\nfunction increment() {\n  count.value++;\n  state.items.push(count.value);\n}\n\n// 测试 watchEffect\nimport { watchEffect } from 'vue';\n\nwatchEffect(() => {\n  console.log('Count changed:', count.value);\n});\n\n// 测试生命周期\nimport { onMounted, onUnmounted } from 'vue';\n\nonMounted(() => {\n  console.log('Component mounted');\n});\n\nonUnmounted(() => {\n  console.log('Component unmounted');\n});\n\n// 测试 defineProps 和 defineEmits\nconst props = defineProps({\n  title: String,\n  count: Number,\n});\n\nconst emit = defineEmits(['update', 'delete']);\n\nfunction handleUpdate() {\n  emit('update', { value: count.value });\n}\n\n// 导出供测试\nexport { count, doubleCount, emit, handleUpdate, increment, message, props, state };\n"
  },
  {
    "level": "error",
    "message": "The useUnmounted hook must be called at the top level, not inside loops, conditions, or nested functions.",
    "file": "__fixtures__/parse/script-only/input.ts",
    "line": 39,
    "column": 0,
    "source": "// 测试 Vue Composition API 转换为 React Hooks\n// 注意，这样的写法转成 React 完全不符合规则，仅测试用。\n\nimport { computed, reactive, ref } from 'vue';\n\n// 测试 ref\nconst count = ref(0);\nconst message = ref('Hello');\n\n// 测试 computed\nconst doubleCount = computed(() => count.value * 2);\n\n// 测试 reactive\nconst state = reactive({\n  name: 'Test',\n  items: [1],\n});\n\n// 测试函数\nfunction increment() {\n  count.value++;\n  state.items.push(count.value);\n}\n\n// 测试 watchEffect\nimport { watchEffect } from 'vue';\n\nwatchEffect(() => {\n  console.log('Count changed:', count.value);\n});\n\n// 测试生命周期\nimport { onMounted, onUnmounted } from 'vue';\n\nonMounted(() => {\n  console.log('Component mounted');\n});\n\nonUnmounted(() => {\n  console.log('Component unmounted');\n});\n\n// 测试 defineProps 和 defineEmits\nconst props = defineProps({\n  title: String,\n  count: Number,\n});\n\nconst emit = defineEmits(['update', 'delete']);\n\nfunction handleUpdate() {\n  emit('update', { value: count.value });\n}\n\n// 导出供测试\nexport { count, doubleCount, emit, handleUpdate, increment, message, props, state };\n"
  }
]
