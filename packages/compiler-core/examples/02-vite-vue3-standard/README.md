# 02-vite-vue3-standard

本示例是标准 `vue-ts` 风格工程，用于验证常规 Vite Vue3 项目的闭环转换。

## 运行

```bash
npm install

# vue 构建
npm run build

# vureact 构建
npm run vr:build

# vureact 构建
npm run vr:wa

## 覆盖能力
```

- `ref/computed` 与基础事件
- 组件 props + 插槽
- 基础样式导入
- Vue 入口排除策略（`src/main.ts`）
